# secrets + per-project config
.env
# TTS venv
.kokoro-venv/
# intermediates — keep only scripts; regenerate the rest
seg_*.wav
narration.wav
*.webm
offset.json
timing.json
*.srt
*.vtt
build/
modules/
node_modules/
# the per-project module config you copy from modules.example.mjs
modules.mjs
